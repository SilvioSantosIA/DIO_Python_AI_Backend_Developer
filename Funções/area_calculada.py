def calcular_area_retangulo(base, altura):
    area = base * altura
    return area

base = 5
altura = 3

area_calculada = calcular_area_retangulo(base, altura) 
print(f"A área do retângulo é {area_calculada}")